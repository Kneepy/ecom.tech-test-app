import React from "react";
interface Props {
    onClick?: () => void;
    active?: boolean;
    children?: React.ReactNode;
}
export declare const Filter: ({ onClick, children, active }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
